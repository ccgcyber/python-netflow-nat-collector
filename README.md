# Python Netflow Nat Collector

A small daemon for obtaining data on NAT-translations by Netflow v9 and storing the data in PostgreSQL.

# Установка

    virtualenv venv
    ./venv/bin/pip install --editable .
